addons:
  apt:
    packages:
    - lftp
    - lib32z1
    - lib32stdc++6

language: c

sudo: false

env:
  - VERSION=1.9
  - VERSION=1.10


script:
  - 'if [[ $TRAVIS_BRANCH == "master" ]]; then bash travisCI/test.sh $VERSION $TRAVIS_BRANCH; fi'

notifications:
    email: false